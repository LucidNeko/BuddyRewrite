#ifndef IO_H
#define IO_H

#include <string>

#include "types.h"

class IO
{
public:
    static std::string assetDirectory();

//    static bool loadImage(std::string fname, U32& width, U32& height, U8& bits);
};

#endif // IO_H
